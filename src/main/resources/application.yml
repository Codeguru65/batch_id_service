 spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/batchIdCheck"
    username: postgres
    password: S3np4i#187
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
